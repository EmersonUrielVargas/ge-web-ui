export interface UserCredentials {
    email:string;
    password: string;
}

export interface UserRegisterInfo {
    name:string;
    email:string;
    password: string;
}

export interface UserData {
    id: number,
    name: string,
    email: string
}